# Copy this to .env.local for local development.
# In production builds, leave unset to use the default (https://relay.savehop.tovix.nl),
# or set to point at your own self-hosted relay.

VITE_SERVER_URL=http://localhost:8787
