# Copy this to .env.local for local development.
# In production builds, leave unset to use the default (https://savehop.fly.dev),
# or set to point at your own self-hosted relay.

VITE_SERVER_URL=http://localhost:8787
